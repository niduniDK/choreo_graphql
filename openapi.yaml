openapi: 3.0.1
info:
  title: /
  version: 0.1.0
servers:
- url: "{server}:{port}"
  variables:
    server:
      default: http://localhost
    port:
      default: "9090"
paths:
  /:
    post:
      operationId: getGreeting
      responses:
        "200":
          description: Ok
          content:
            text/plain:
              schema:
                type: string


  /user:
    post:
      operationId: getUser
      requestBody:
        required: true
        content:
          text/plain:
            schema:
              type: string
      responses:
        "200":
          description: Ok
          content:
            text/plain:
              schema:
                type: string